class Solution {
  /**
     * @param {string[]} strs
     * @returns {string}
     */
  encode(strs) {
    // let result = '';
    // if (strs.length === 0) return result;
    // for (let i = 0; i < strs.length; i++) {
    //   result += strs[i].length + '_' + strs[i];
    // }
    // return result;
  }

  /**
     * @param {string} str
     * @returns {string[]}
     */
  decode(str) {
    // let result = [];
    // for (let i = 0; i < strs.length; i++) {
    //     const count = strs[i];
    //     result.puhs
    // }
    // return result;
  }
}

module.exports = { Solution };