Мав live coding assessment в місцеву фірму, година кодингу, ось такі задачки:

1. Write a function that takes a string as input and returns the reversed string.
Example:
Input: "hello" → Output: "olleh"

2. Given a string s consisting of words and spaces, return the length of the last word in the string.
Input: s = "   fly me   to   the moon  "
Output: 4

3. Given a string containing just the characters '(', ')', '{', '}', '[', and ']', determine if the input string is valid (i.e., open brackets are closed in the correct order).
Example: "({[]})" → valid, "([)]" → invalid

(){}[]
([)]
valid => (){}[]
invalid => ([)]


4. Implement a function to calculate the nth Fibonacci number using memoization
1,1,2,3,5,8,13,21
10 => 55


5. Given a string s and a non-empty string p, return all the start indices of p’s anagrams in s
Example:
Input: s = "cbaebabacd", p = "abc"
Output: [0, 6] (Anagrams: "cba", "bac")